# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:알파벳 쓰기 순발력 게임

이 프로그램을 만든 이유:평소에 영어 타자가 느리기도 하고 어느 알파벳이 어느 위치에 있는지 햇깔릴때도있, 친구와 함께한다면 순발력도 기르고 협동심도 기르려고 만들게 되었다.


---

## 2. 문제 분해 및 모델링

### 입력
시스템 자동 입력: 97(a)부터 122(z) 사이의 무작위 아스키코드로 생성된 목표 소문자 알파벳 2개

사용자 키보드 입력: 플레이어 1과 플레이어 2가 각각 타건하여 입력한 알파벳 문자열

시간 데이터: 각 플레이어가 입력을 시작한 시간과 엔터를 눌러 입력을 마친 시간


### 처리
시간 계산: (입력 종료 시간) - (입력 시작 시간)을 계산하여 플레이어별 소요 시간을 구함

조건 판별 (실패/성공):

1.소요 시간이 현재 제한시간을 초과했는지 확인 (시간 초과 판별)

2.플레이어의 입력값이 각자 할당된 목표 알파벳과 정확히 일치하는지 비교 (오답 판별)

난이도 조절: 라운드를 통과할 때마다 다음 라운드의 제한시간을 0.5초씩 감소시킴 (단, max() 함수를 통해 최소 1.0초 보장)

데이터 누적: 라운드 종료 시마다 [라운드 번호, 목표 알파벳, P1 입력, P2 입력, 결과] 구조의 데이터를 2차원 리스트(game_records)에 한 행씩 추가


### 출력
진행 상황 안내: 현재 라운드 번호, 화면에 제시된 목표 알파벳 2개, 현재 제한시간 메세지

결과 피드백: 라운드 통과 성공 메세지 및 현재 누적 점수

최종 데이터 표: 게임 오버 시 2차원 리스트에 저장된 전체 라운드의 게임 기록을 깔끔한 표 형태로 터미널 창에 출력

최종 점수: 게임 종료 시 플레이어들이 달성한 최종 스코어 출력

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:game_records

한 행의 의미:하나의 라운드에서 발생한 모든 플레이 데이터(라운드 번호, 목표 알파벳, 두 플레이어의 입력값, 게임 결과)를 한 줄로 묶은 전체 기록입니다. 매 라운드가 끝날 때마다 이 행이 한 줄씩 추가됩니다.

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 라운드 번호 (현재 라운드) | 1 |
| 1번 열 | 제시된 알파벳 (목표 소문자 2개) | "a, z" |
| 2번 열 | 플레이어 1이 실제 입력한 값 | "a" |
| 3번 열 | 플레이어 2가 실제 입력한 값 | "z" |

이 2차원 리스트를 사용한 이유:게임이 몇 라운드까지 진행될지 예측할 수 없는 상황에서, 라운드마다 발생하는 여러 개의 연관된 데이터를 흩어지지 않게 하나의 행으로 묶어  누적하기 위해서. 또한, 게임이 종료된 후 for 반복문을 사용해 전체 기록을 깔끔한 표 형태로 한 번에 출력하기에 가장 효율적인 구조이기 때문


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| time.time() | 현재 시간을 소수점 단위(초)로 측정하여 소요 시간 계산에 활용 | 없음 | 현재 시간을 나타내는 실수 값 |
| random.randint() | 아스키코드 범위 내에서 무작위 정수를 뽑아 랜덤 알파벳을 생성 | 최소 정수, 최대 정수 | 범위 내의 무작위 정수 |
| max() | 시간 단축 시 제한시간이 일정 수준 이하로 떨어지지 않도록 방어 | 비교할 숫자들 | 입력된 값 중 가장 큰 숫자 |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: time.time()

설명: 단순히 제한된 시간내에 알파벳을 입력하는 것이 아닌 제한시간이 점점 줄어들며 빠른 타자를 유발하며 긴장감을 주고 흥미를 유발시킬수 있다.

---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
1. 제한시간 초과 여부: 플레이어들이 입력을 완료하는 데 걸린 소요 시간이 현재 라운드에 설정된 제한시간보다 큰지 검사했습니다 (예: p1_duration > limit_time).
2. 정답 일치 여부: 두 플레이어가 키보드로 입력한 값이 시스템이 제시한 목표 알파벳과 정확히 일치하는지 논리 연산자(==, and)를 사용하여 검사했습니다.


### 반복문 사용 부분

무엇을 반복 처리했는가?
1. 메인 게임 루프 (while True): 게임 오버 조건에 걸리기 전까지 '알파벳 무작위 생성 -> 사용자 입력 -> 정답 판별 및 시간 단축'이라는 게임의 전체 흐름을 무한히 반복 실행했습니다.
2. 게임 기록 출력 루프 (for문): 게임이 종료된 후, 2차원 리스트(game_records) 안에 누적된 데이터들을 한 줄씩 차례대로 꺼내어 터미널 창에 표 형태로 그리는 작업을 반복했습니다.


### 조건문과 반복문이 함께 작동한 부분

반복문(while True)이 게임을 끊임없이 다음 라운드로 진행시키는 동안, 그 내부에서 조건문(if)이 매 라운드마다 플레이어의 상태를 감시하며 방향을 결정합니다. 조건문이 '성공'을 판단하면 반복문이 계속 돌아가도록 놔두지만, '오답'이나 '시간 초과' 조건을 감지하는 순간 내부에서 break 명령어를 실행하여 반복문의 흐름을 즉시 끊고 게임을 종료(게임 오버)시키는 방식으로 두 기능이 밀접하게 상호작용합니다.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text================= 게임 기록 =================
[라운드] |  [정답]  | [P1 입력] | [P2 입력] | [결과]
-----------------------------------------------
   1     |   h, m  |     h     |     m     | SUCCESS
   2     |   a, g  |     a     |     g     | SUCCESS
   3     |   p, y  |     p     |     y     | SUCCESS
   4     |   v, n  |     v     |     n     | SUCCESS
   5     |   d, i  |     dsew           | TIME OVER (P2)
===============================================
최종 점수: 4점

```

결과 분석:진행한 라운드와 입력한 정답, 처음 입력한 답과 두번째로 입력한 답을 출력하고 실패인지 성공인지 표시한다 


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:a,n  b,c 등등

예상 결과: 2     |   a, g  |     a     |     g     | SUCCESS,   1     |   c, p  |     b    |     c     | FAIL

실제 결과:2     |   a, g  |     a     |     g     | SUCCESS,   1     |   c, p  |     b    |     c     | FAIL


### 테스트 2: 예외 또는 경계 사례
예외)
입력: 화면에 제시된 목표가 소문자 a일 때, 플레이어가 대문자 A를 입력.

예상 결과: 파이썬은 대소문자를 엄격하게 구분하므로, 오답 처리 후 게임이 즉시 종료되어야 함.

실제 결과: 파이썬에서 'a' == 'A'는 False입니다. 따라서 정답 조건문을 통과하지 못하고 else 구문으로 넘어가 "[게임 오버] 알파벳을 잘못 입력했습니다!"가 출력되며 의도대로 게임이 종료됩니다. (예상 결과와 일치)
경계)
입력: 플레이어들이 9번 연속으로 정답을 맞혀서 10라운드에 진입함. (초기 5.0초에서 매 라운드 0.5초씩 9번 감소하면 단순 계산상 0.5초가 되어야 하는 상황)

예상 결과: max(1.0, ...) 방어 로직이 정상 작동하여 제한 시간이 1.0초 밑으로 떨어지지 않아야 함.

실제 결과: 9라운드 성공 후 limit_time = max(1.0, 1.5 - 0.5)가 계산되어 값은 1.0이 됩니다. 그 이후 라운드에서도 max(1.0, 1.0 - 0.5)는 계속 1.0을 반환합니다. 따라서 10라운드 이후부터는 계속 "⏰ 현재 제한시간: 1.0초"라고 정상적으로 출력됩니다. (예상 결과와 일치)

### 발견한 오류와 수정한 방법

import 를 넣어야하는 부분에서 넣지 않아 코드가 작동하지 않았음
---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:2. 기능 고도화 및 규칙 수정 (버전 업데이트)
입력 규칙 강화: 알파벳 소문자만 입력하도록 제한하고, 자동 소문자 변환(데이터 전처리) 기능을 제거하여 오타 발생 시 즉각 실패 처리되도록 난이도를 조정함.

게임 루프 수정: 5라운드 제한 방식에서 벗어나, 오답이나 시간 초과가 발생하기 전까지 무한 반복(while True)되는 서바이벌 형식으로 규칙을 변경함.

동적 난이도 적용: 게임에 긴장감을 부여하기 위해 라운드 통과 시마다 제한시간을 0.5초씩 단축시킴. 단, max() 함수를 활용하여 최소 제한시간(1.0초)의 하한선을 설정함.

3. 디버깅 및 오류 해결
모듈 오류 수정: 코드 실행 중 발생한 NameError: name 'random' is not defined 에러의 원인이 모듈 미호출에 있음을 파악하고, import random 및 전체 코드를 수정하여 정상 작동하도록 조치함.

4. 핵심 프로그래밍 개념 적용 및 분석
반복문 활용: while True를 통한 메인 게임 루프 구현 및 for문을 활용한 최종 게임 기록 표 출력 과정을 분석함.

조건문 활용: 소요 시간 초과 여부(p1_duration > limit_time) 및 정답 일치 여부 검사 등 논리 연산의 역할을 정리함.

제어문 상호작용: 반복문이 게임을 진행시키는 동안 내부의 조건문이 성공/실패 여부를 판단하고, 실패 시 break를 통해 루프를 탈출하는 유기적 구조를 문서화함.

5. 문서화 및 보고서 작성 지원
문제 분해 및 모델링: 프로그램의 전체 흐름을 '입력 - 처리 - 출력' 3단계로 구조화하여 문서 형식으로 정리함.

자료 구조 명세서: 2차원 리스트(game_records)의 행과 열이 가지는 의미, 데이터 타입, 실제 예시 및 채택 사유를 표 형식으로 작성함.

함수 명세서: play_game, random.randint, time.time, max 등 프로그램에 사용된 주요 함수의 역할, 입력값, 반환값을 정리하고, 가장 핵심적인 함수(time.time)의 작동 원리를 구체적으로 서술함.

프롬프트 기록 추출: 과제 및 보고서 증빙을 위해 목적별, 시간순으로 AI에게 입력한 질문(프롬프트) 내역을 수집하고 복사하기 쉬운 형태로 텍스트 블록화함.


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:

* 자동 대소문자 변환 기능 (데이터 전처리): AI는 사용자가 대문자를 입력하더라도 자동으로 소문자로 변경하여 정답을 인정해 주는 방식을 제안했으나, 게임의 난이도와 긴장감을 높이기 위해 이를 거부하고 사용자가 반드시 소문자로만 정확히 입력해야 하도록 규칙을 직접 수정함.
* 고정된 라운드 판정 시스템: 초기 코드 제안 단계에서 특정 횟수(예: 5라운드)만 진행하고 종료되는 방식을 제시받았으나, 플레이어의 도전 욕구를 자극하기 위해 이를 제외하고 게임 오버 전까지 끝없이 지속되는 방식으로 변경함.


최종 코드에서 내가 직접 판단하거나 수정한 부분:

* 서바이벌 방식의 게임 룰 전환: 플레이어 1과 플레이어 2가 각각 알파벳을 하나씩 나누어 입력하고, 단 한 번의 실수(오답 또는 시간 초과)로도 즉시 게임이 끝나는 무한 반복 형태의 서바이벌 구조를 직접 판단하여 반영함.
* 점진적 제한시간 단축 기획 (동적 난이도 조절): 게임이 뒤로 갈수록 지루해지는 것을 방지하기 위해, 라운드를 성공할 때마다 제한시간이 줄어들도록 만드는 난이도 상승 아이디어를 직접 착안하고 코드에 구현하도록 요청함.
* 엄격한 오타 판정 기준 수립: 입력값 전처리 로직을 삭제함으로써 사용자의 사소한 대소문자 입력 오타도 실력의 요소로 포함시키도록 게임의 밸런스를 직접 결정함.
---

## 9. 한계와 개선 방안

현재 프로그램의 한계:

* 콘솔(CUI) 환경의 시각적 한계: 텍스트 기반의 터미널 창에서만 실행되므로 시각적인 몰입감이 떨어지고, 실시간으로 줄어드는 타이머 바 등을 화면에 표시하기 어려움.
* 순차적 입력 처리의 한계: 파이썬 `input()` 함수의 특성상 플레이어 1이 입력을 마치고 엔터를 누른 후에야 플레이어 2의 타이머와 입력 창이 활성화됨. 두 플레이어가 동시에 키보드를 두드리며 경쟁하는 완전한 실시간 동시 입력이 불가능함.
* 데이터의 휘발성: 게임 기록을 메모리 내부의 2차원 리스트에만 저장하므로, 프로그램이 종료되면 그동안 쌓인 역대 최고 점수나 플레이 기록이 모두 삭제됨.


다시 개선한다면 추가하고 싶은 기능:

* 멀티스레딩 기반 동시 입력: 비동기 처리나 스레딩 기법을 도입하여 두 플레이어가 순서를 기다리지 않고 한 화면에서 동시에 알파벳을 입력하여 경쟁하도록 개선함.
* 파일 입출력을 활용한 데이터 영구 저장: 게임 종료 시 기록을 CSV 파일이나 텍스트 파일로 로컬 컴퓨터에 자동 저장하여, 누적 스코어를 기반으로 한 '스코어보드'을 구축
* 다양한 아이템 및 모드 도입: 특정 라운드마다 제한시간을 늘려주는 보너스 알파벳이나, 단어가 제시되는 하드코어 모드 등을 추가하여 게임의 다양성을 확보함.

---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
입력: 시스템이 생성한 랜덤 알파벳, 플레이어의 키보드 입력 값, 입력 시작/종료 시간

처리: 플레이어별 소요 시간 계산, 정답 및 시간 초과 판별, 라운드 통과 시 제한시간 단축(최소 1.0초 보장)

출력: 라운드별 목표 및 타이머 안내, 게임 오버 시 전체 기록 표(스코어보드) 및 최종 점수
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
구조: 행(Row)은 '각 라운드의 전체 기록'을 뜻하며, 열(Column)은 '라운드 번호 / 목표 알파벳 / P1 입력 / P2 입력 / 결과'로 구성됨

사용 이유: 게임이 끝날 때까지 과거의 성공 기록을 유실 없이 누적 보존하고, 마지막에 일괄 표 형태로 출력하기 위함
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
[time.time()]역할 및 중요성: 현재 시간을 초 단위 실수로 정밀 측정함. 단순 타자 연습이 아닌 소요 시간을 기반으로 한 '시간 초과 판정'과 '동적 난이도 조절(시간 압박)'을 가능하게 만드는 게임의 핵심 엔진임
4. 조건문과 반복문은 어디에서 사용되었는가?
반복문 (while, for): 탈락 전까지 게임을 무한 진행시키고(while True), 종료 후 누적 데이터를 한 줄씩 출력함(for)

조건문 (if): 매 라운드 입력값의 정답 여부와 시간 초과 여부를 감시함

상호작용: 반복문이 게임을 굴리다가, 조건문이 오답이나 시간 초과를 감지하는 순간 break를 실행해 반복을 멈추고 게임을 종료시킴
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
AI 제안: 대소문자 자동 변환 편의 기능, 5라운드 제한 판정 시스템 제시

사용자 직접 수정: 편의 기능 삭제(소문자 대문자 구분), 제한 없는 무한 모드로 전환, 라운드 성공 시마다 시간이 줄어드는 기획 반영