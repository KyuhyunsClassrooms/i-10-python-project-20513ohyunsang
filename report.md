# 최종 보고서

## 1. 프로그램 주제와 목적

내 프로그램의 주제:알파벳 쓰기 순발력 게임

이 프로그램을 만든 이유:평소에 영어 타자가 느리기도 하고 어느 알파벳이 어느 위치에 있는지 했깔릴때도 있고, 친구와 함께한다면 순발력도 기르고 협동심도 기르려고 만들게 되었다.


---

## 2. 문제 분해 및 모델링

### 입력
시스템 자동 입력: 97(a)부터 122(z) 사이의 무작위 아스키코드로 생성된 목표 소문자 알파벳 2개

사용자 키보드 입력: 플레이어 1과 플레이어 2가 각각 타건하여 입력한 알파벳 문자열

시간 데이터: 각 플레이어가 입력을 시작한 시간과 엔터를 눌러 입력을 마친 시간


### 처리
시간 계산: (입력 종료 시간) - (입력 시작 시간)을 계산하여 플레이어별 소요 시간을 구함

조건 판별 (실패/성공):

1.소요 시간이 현재 제한시간을 초과했는지 확인 (시간 초과 판별)

2.플레이어의 입력값이 각자 할당된 목표 알파벳과 정확히 일치하는지 비교 (오답 판별)

난이도 조절: 라운드를 통과할 때마다 다음 라운드의 제한시간을 0.5초씩 감소시킴 (단, max() 함수를 통해 최소 1.0초 보장)

데이터 누적: 라운드 종료 시마다 [라운드 번호, 목표 알파벳, P1 입력, P2 입력, 결과] 구조의 데이터를 2차원 리스트(game_records)에 한 행씩 추가


### 출력
진행 상황 안내: 현재 라운드 번호, 화면에 제시된 목표 알파벳 2개, 현재 제한시간 메세지

결과 피드백: 라운드 통과 성공 메세지 및 현재 누적 점수

최종 데이터 표: 게임 오버 시 2차원 리스트에 저장된 전체 라운드의 게임 기록을 깔끔한 표 형태로 터미널 창에 출력

최종 점수: 게임 종료 시 플레이어들이 달성한 최종 스코어 출력

---

## 3. 사용한 2차원 리스트 설명

2차원 리스트 이름:game_records

한 행의 의미:하나의 라운드에서 발생한 모든 플레이 데이터(라운드 번호, 목표 알파벳, 두 플레이어의 입력값, 게임 결과)를 한 줄로 묶은 전체 기록입니다. 매 라운드가 끝날 때마다 이 행이 한 줄씩 추가됩니다.

각 열의 의미:

| 열 번호 | 의미 | 실제 예시 |
|---:|---|---|
| 0번 열 | 라운드 번호 (현재 라운드) | 1 |
| 1번 열 | 제시된 알파벳 (목표 소문자 2개) | "a, z" |
| 2번 열 | 플레이어 1이 실제 입력한 값 | "a" |
| 3번 열 | 플레이어 2가 실제 입력한 값 | "z" |

이 2차원 리스트를 사용한 이유:게임이 몇 라운드까지 진행될지 예측할 수 없는 상황에서, 라운드마다 발생하는 여러 개의 연관된 데이터를 흩어지지 않게 하나의 행으로 묶어  누적하기 위해서. 또한, 게임이 종료된 후 for 반복문을 사용해 전체 기록을 깔끔한 표 형태로 한 번에 출력하기에 가장 효율적인 구조이기 때문


---

## 4. 함수 설명

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| time.time() | 현재 시간을 소수점 단위(초)로 측정하여 소요 시간 계산에 활용 | 없음 | 현재 시간을 나타내는 실수 값 |
| random.randint() | 아스키코드 범위 내에서 무작위 정수를 뽑아 랜덤 알파벳을 생성 | 최소 정수, 최대 정수 | 범위 내의 무작위 정수 |
| max() | 시간 단축 시 제한시간이 일정 수준 이하로 떨어지지 않도록 방어 | 비교할 숫자들 | 입력된 값 중 가장 큰 숫자 |

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: time.time()

설명: 단순히 제한된 시간내에 알파벳을 입력하는 것이 아닌 제한시간이 점점 줄어들며 빠른 타자를 유발하며 긴장감을 주고 흥미를 유발시킬수 있다.

---

## 5. 조건문과 반복문 활용

### 조건문 사용 부분

어떤 조건을 검사했는가?
1. 제한시간 초과 여부: 플레이어들이 입력을 완료하는 데 걸린 소요 시간이 현재 라운드에 설정된 제한시간보다 큰지 검사했습니다 (예: p1_duration > limit_time).
2. 정답 일치 여부: 두 플레이어가 키보드로 입력한 값이 시스템이 제시한 목표 알파벳과 정확히 일치하는지 논리 연산자(==, and)를 사용하여 검사했습니다.


### 반복문 사용 부분

무엇을 반복 처리했는가?
1. 메인 게임 루프 (while True): 게임 오버 조건에 걸리기 전까지 '알파벳 무작위 생성 -> 사용자 입력 -> 정답 판별 및 시간 단축'이라는 게임의 전체 흐름을 무한히 반복 실행했습니다.
2. 게임 기록 출력 루프 (for문): 게임이 종료된 후, 2차원 리스트(game_records) 안에 누적된 데이터들을 한 줄씩 차례대로 꺼내어 터미널 창에 표 형태로 그리는 작업을 반복했습니다.


### 조건문과 반복문이 함께 작동한 부분

반복문(while True)이 게임을 끊임없이 다음 라운드로 진행시키는 동안, 그 내부에서 조건문(if)이 매 라운드마다 플레이어의 상태를 감시하며 방향을 결정합니다. 조건문이 '성공'을 판단하면 반복문이 계속 돌아가도록 놔두지만, '오답'이나 '시간 초과' 조건을 감지하는 순간 내부에서 break 명령어를 실행하여 반복문의 흐름을 즉시 끊고 게임을 종료(게임 오버)시키는 방식으로 두 기능이 밀접하게 상호작용합니다.

---

## 6. 실행 결과

대표 실행 결과를 붙여넣으세요.

```text

```

결과 분석:


---

## 7. 테스트와 오류 수정

### 테스트 1: 정상 사례

입력:

예상 결과:

실제 결과:


### 테스트 2: 예외 또는 경계 사례

입력:

예상 결과:

실제 결과:


### 발견한 오류와 수정한 방법


---

## 8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:


Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:


최종 코드에서 내가 직접 판단하거나 수정한 부분:


---

## 9. 한계와 개선 방안

현재 프로그램의 한계:


다시 개선한다면 추가하고 싶은 기능:


---

## 10. 코드인터뷰 준비

다음 질문에 답할 수 있도록 준비하세요.

1. 내 프로그램의 입력-처리-출력은 무엇인가?
2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?
3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?
4. 조건문과 반복문은 어디에서 사용되었는가?
5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?
